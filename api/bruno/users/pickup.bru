meta {
  name: ピックアップユーザー一覧取得
  type: http
  seq: 1
}

get {
  url: {{base_url}}/users/pickup
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

vars:pre-request {
  base_url: http://localhost:8080
}
